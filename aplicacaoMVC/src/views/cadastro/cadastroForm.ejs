<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Document</title>
    <style>
        li {
            padding-bottom: 20px;
        }
    </style>
</head>
<body>    
        <form action="/home/save" method="POST">
            <div id="Formulário para cadastro" class="form-group">
                <label for="nome">Nome:</label>
                <input type="text" id="nome" name="nome" class="form-group" placeholder="Nome" required>
                
                <label for="email">Email:</label>
                <input type="text" id="email" name="email" class="form-group" placeholder="Email" required>
                
                <button class="btn btn-primary" type="submit">Cadastrar</button>
            </div>
        </form>

        <div>
            <h4>Lista de Cadastros:</h4>
                <% if (lista.length > 0) { %>
                    <ul>
                        <% lista.forEach(cadastro => { %>
                            <form action="/excluir/<%= cadastro.email %>?_method=DELETE" method="POST" onsubmit="return confirm('Tem certeza que deseja excluir?');">
                                <li><%= cadastro.nome %> - <%= cadastro.email %>
                                    <button type="submit">Excluir</button>
                                </li>
                            </form>
                        <% }) %>
                    </ul>
                    <h1>Total de cadastros: <%= totalCadastro %></h1>
                <% } else { %>
                    <p>Nenhum cadastro.</p>
                <% } %>
        </div>

</body>
</html>
